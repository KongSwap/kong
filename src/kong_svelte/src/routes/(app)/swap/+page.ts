// Swap page configuration
// This is a critical route that should be prerendered for better performance
export const prerender = true;

// Disable SSR since we're using adapter-static
export const ssr = false; 